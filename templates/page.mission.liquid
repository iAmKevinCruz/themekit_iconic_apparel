<div class="mission_content_body">
      {% comment %}<div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>{% endcomment %}

  {% section 'mission-rich-text' %}
  {% section 'mission-timer' %}
  {% section 'mission-logo-bar' %}
</div>